import '../data/models/payment_model.dart';

/// High-level payment service to orchestrate payments and receipts.
class PaymentService {
  PaymentService();

  Future<void> processPayment(PaymentModel payment) async {
    // Placeholder: validate, persist and notify
  }
}
